spec_version: 1
name: sre_incident_env
type: space
runtime: fastapi
app: server.app:app
port: 7860

tasks:
  - id: easy
    description: "Payment API elevated 500s after deploy — rollback required"
    difficulty: easy
    grader: POST /grader
  - id: medium
    description: "Database connection pool exhaustion — scale pgbouncer and page db-team"
    difficulty: medium
    grader: POST /grader
  - id: hard
    description: "CDN misconfiguration global latency spike — disable feature flag"
    difficulty: hard
    grader: POST /grader
